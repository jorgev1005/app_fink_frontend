const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const contacts = await prisma.contactPerson.findMany(); const findContact = (id) => contacts.find(c => c.id === id); const cFruty = contacts.find(c => c.name && c.name.toLowerCase().includes('fruty')); const fVendor = cFruty ? await prisma.invoice.findMany({ where: { vendorId: cFruty.id }, include: { payments: true } }) : []; console.log('--- FRUTY ---'); console.log(JSON.stringify(fVendor, null, 2)); const cJhonny = contacts.find(c => c.name && c.name.toLowerCase().includes('jhonny')); const j = cJhonny ? await prisma.invoice.findMany({ where: { OR: [{ customerId: cJhonny.id }, { vendorId: cJhonny.id }] }, include: { payments: true } }) : []; console.log('--- JHONNY ---'); console.log(JSON.stringify(j, null, 2)); const r = await prisma.recurringRule.findMany({ where: { description: { contains: 'Alquiler' } } }); console.log('--- ALQUILER ---'); console.log('REGLAS:', JSON.stringify(r, null, 2)); const rInstances = await prisma.invoice.findMany({ where: { recurringRuleId: { in: r.map(x => x.id) } }, include: { payments: true } }); console.log('INSTANCIAS ALQUILER:', JSON.stringify(rInstances, null, 2)); const singleInv = await prisma.invoice.findMany({ where: { code: 'INV-547235' }, include: { payments: true }}); console.log('FACTURA ESPECIFICA INV-547235:', JSON.stringify(singleInv, null, 2)); } main().finally(() => prisma.\$\disconnect());

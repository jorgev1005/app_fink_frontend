const fs = require('fs'); let c = fs.readFileSync('scripts/fix_lucem_ca_petty_cash.js', 'utf8'); c = c.replace(/    const bsDelta \= hasBs \? round2\(0 \- ledgerBalances\.balanceBs\) \: 0;\\n      projectId: exactAccount\.projectId,/g, '    const bsDelta = hasBs ? round2(0 - ledgerBalances.balanceBs) : 0;\\n    const equityAccount = await prisma.account.findFirst({\\n      where: {\\n        projectId: exactAccount.projectId,'); fs.writeFileSync('scripts/fix_lucem_ca_petty_cash.js', c);

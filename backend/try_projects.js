const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function m() { console.log(await prisma.project.findMany({ select: { id: true, name: true, code: true } })); } m();

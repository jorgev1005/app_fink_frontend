const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function fix() { const accs = await prisma.account.findMany({ where: { name: { in: ['Exchange Binance', 'Caja Chica USD', 'Banco de Venezuela USD', 'Billetera Neomoon', 'Tarjeta Neomoon', 'Comisiones en USD'] } } }); for (let a of accs) { await prisma.account.update({ where: { id: a.id }, data: { currency: 'USD' } }); console.log('Fixed:', a.name); } } fix().finally(() => prisma.$disconnect());

const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.transaction.findMany({ where: { status: 'COMPLETED', OR: [{type: 'EXPENSE'}, {type: 'PAYMENT'}] }, select: { id: true, type: true, categoryRef: { select: {name: true} }, category: true, amountBs: true } }).then(tx => console.log('Expenses:', tx.filter(t=>t.type==='EXPENSE').reduce((acc, t)=>acc+Number(t.amountBs), 0), 'Payments:', tx.filter(t=>t.type==='PAYMENT').reduce((acc, t)=>acc+Number(t.amountBs), 0))).finally(()=>prisma.$disconnect());
